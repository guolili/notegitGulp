git安装过程中如果想要让git命令正确运行在windows系B统上，需要选择下面哪项？

use git from the windows command prompt


、、、、、、、、、、、、、、
git的版本可以在本地创建，但是不可以从远程拉取


、、、、、、、、、、、、、、、、、
git的ssh协议和https协议的区别

使用https协议每次和远程进行交互时需要输入git服务器账户名和密码


，，，，，，，，，，，，，，，，，，
将工作区的文件提交到暂存区命令是    git add .

，，，，，，，，，，，，，，，，，，，，，，，，，，
git 将暂存区的文件进行删除的操作是          git stash drop

，，，，，，，，，，，，，，，，，，，
git从远程更新本地代码的方式有

git fetch          git pull



github的介绍正确的是：

github是一个开源的平台；
github是代码可以面向开源，也可以进行付费私有管理



操纵git命令时，可以直接使用windows的cmd进行，在安装时不需要进行任何特殊的配置，默认安装即可；F
git的只有一份版本库，存放在服务器上，服务器一旦宕掉，所有版本将不存在T
git使用ssh进行代码管理时，每次和服务器交互都必须进行用户名和密码的验证F
git log用来查询暂存区的所有的信息F
git reset命令是用来恢复到最近一次提交过的状态，即放弃上次提交后的所有本次修改；F
git rm 从版本库中删除文件T